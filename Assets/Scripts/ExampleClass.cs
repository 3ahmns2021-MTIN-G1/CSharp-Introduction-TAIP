// Bibliotheken / Asembly Referenz / Namespaces

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Deklaration einer Klasse

// public           -> access modifier (Zugriffsrechte) | keyword (vorrreservierte Schlüsselbegriffe)
// class            -> keyword zur Deklaration einer Klasse
// ExampleClass     -> identifier (Name der Klasse)
// :MonoBehavior    -> Erstellen eine Unterjlasse von MonoBehaviour
// {}               -> Scope/Wirkungsbereich (Was ist alles inb der Klasse enthalten)

public class ExampleClass : MonoBehaviour
{
    
}
